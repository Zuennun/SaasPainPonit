# Automated discovery holdout

Status: **NOT_RUN_CONFIGURATION_REQUIRED**. No unseen holdout inference has run.

Blocker: OPENAI_API_KEY, DISCOVERY_MODEL, ARCTIC_SHIFT_USER_AGENT or --user-agent, explicit rights confirmation for third-party LLM processing.

No examples or quality recommendation are asserted without real model calls.
Semantic recurrence remains a separate clustering gap; thresholds are unchanged.
