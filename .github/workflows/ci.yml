name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e '.[dev]'
      - run: ruff check .
      - run: pyright
      - run: python -m unittest discover -s tests -v
      - run: >-
          python -m problem_intelligence.cli evaluation-readiness
          --manifest benchmarks/readiness_v1.json
          > evaluation-readiness.json
